package powerwalk.tasks;

import org.powerbot.core.script.ActiveScript;

/**
 * runs an ActiveScript instance inside of PowerWalk as if it where running in RSBot.
 * <p/>
 * Note that the ActiveScriptRunner might not run the ActiveScript instance in the 
 * exact same way, so it cannot be guaranteed that the provided script will perform 
 * the same.
 * <p/>
 * @author Chronio
 */
public class ActiveScriptRunner {
    
    public ActiveScriptRunner(ActiveScript script) {
        
    }
}
